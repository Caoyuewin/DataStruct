#include <stack>


void PreorderNoR(Node* root){
  stack<Node *> s;
  Node *cur = root;

  while(cur != (!s.empty() || cur != NULL){
      while(cur != NULL) {
      printf("%c", cur->value);
      s.push(cur);
      cur = cur->left;
      }

      Node* top
  }


}
